# Remote-home-assistant-terminal
แทนที่จะควบคุม GPIO โดยตรงบน Raspberry Pi (แบบโปรเจกต์ก่อนหน้า), Raspberry Pi จะทำหน้าที่เป็นโหนดของ Home Assistant หรือเป็นเกตเวย์เท่านั้น

ถ้าอยากเพิ่มเครื่องมือสำหรับจัดการ JSON ใน Bash
```
sudo apt install jq -y
```
